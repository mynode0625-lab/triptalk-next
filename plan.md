# TripTalk — Next.js 이전 계획

> 목표: 현재 순수 HTML/CSS/JS로 만들어진 TripTalk을 **기능 손실 없이 100%** Next.js로 이전한다.
> 작성일: 2026-08-19 · 원본 기준 커밋: `triptalk@0a199b1`
>
> **원본**: `~/ASC/builder` (저장소 `triptalk`) — 읽기 전용 참조
> **작업 대상**: `~/ASC/triptalk-next` (저장소 `triptalk-next`) — 이 문서가 있는 곳
> 세션은 두 폴더 모두 접근 가능한 `~/ASC`에서 여는 것을 권장한다.

---

## 1. 원칙

1. **무손실 이전이 최우선.** 리팩터링·기능 추가·디자인 개선은 이전이 끝난 뒤에 한다.
2. **화면과 동작은 픽셀·거동 단위로 동일해야 한다.** 이전 전후를 나란히 놓고 비교한다.
3. **단계마다 동작하는 상태를 유지한다.** 한 번에 전부 갈아엎지 않는다.
4. **이전 중 발견한 개선점은 코드가 아니라 문서에 적는다.** (본 문서 9장)

---

## 2. 이전 대상 전수 인벤토리

아래 목록이 곧 완료 판정 체크리스트다. 하나라도 빠지면 이전이 끝난 것이 아니다.

### 2.1 랜딩 페이지 — `index.html` + `script.js`(624줄) + `style.css`(644줄)

| # | 기능 | 비고 |
|---|---|---|
| L-01 | 고정 헤더 · 스크롤 시 배경 전환(`is-stuck`) | |
| L-02 | 모바일 햄버거 내비 토글 | |
| L-03 | 현재 섹션 내비 하이라이트 | 스크롤 스파이 |
| L-04 | 히어로 · 배경 blob 애니메이션 + 그리드 | |
| L-05 | 히어로 폰 목업 **채팅 자동 재생** | 타이핑 연출 |
| L-06 | 통계 숫자 **카운트업** 4종 | 뷰포트 진입 시 |
| L-07 | 스크롤 등장 애니메이션(`.reveal`) | IntersectionObserver |
| L-08 | 문제 제기 3카드 | 정적 |
| L-09 | 특징 5카드 (2개는 wide) | 정적 |
| L-10 | **AI 캐릭터 8명** 카드 · 클릭 시 첫 마디 TTS 재생 | 데이터 `CHARACTERS` |
| L-11 | **상황별 학습 탭 5종** (airport/hotel/food/city/trouble) | 데이터 `SITUATIONS` |
| L-12 | **인터랙티브 데모** — 시나리오 선택 + 선택지 분기 대화 + 다시 시작 | 데이터 `DEMO` |
| L-13 | 이용 방법 3단계 | 정적 |
| L-14 | 사용자 후기 4개 | 정적 |
| L-15 | 요금제 3종 + **월간/연간 토글** (가격 치환) | |
| L-16 | **FAQ 아코디언** | 데이터 `FAQS` |
| L-17 | CTA 이메일 폼 (형식 검증 + 안내 문구) | |
| L-18 | 푸터 3열 + 하단 정보 | |
| L-19 | 맨 위로 버튼 (스크롤 임계값) | |

### 2.2 말하기 연습실 — `practice.html` + `practice.js`(1255줄) + `practice.css`(405줄)

**이 파일이 이전 난이도의 90%를 차지한다.** 명령형 상태 기계로 작성돼 있어 React 상태 모델로 재설계가 필요하다.

| # | 기능 | practice.js 구획 |
|---|---|---|
| P-01 | **연습 시나리오 5종** (checkin / immigration / hotel / restaurant / taxi) | §1 |
| P-02 | **발음 교정 사전** — 한국인 취약 발음(r/l, th, f/v, 묵음, 강세) | §2 |
| P-03 | **표현 교정 규칙** — 정중함·관사·콩글리시 등 규칙 기반 | §3 |
| P-04 | 텍스트 비교 유틸 (정규화 · 토큰 diff · 유사도) | §4 |
| P-05 | **TTS 재생** — 내장 음성 + 저품질 음성 필터(BAD/GOOD_VOICE) | §5 |
| P-06 | **클라우드 TTS(선택)** — OpenAI `gpt-4o-mini-tts` / ElevenLabs `eleven_multilingual_v2` + 실패 시 폴백 + 오디오 캐시 | §5 |
| P-07 | **STT 음성 인식** — interimResults, maxAlternatives 3 | §5, §10 |
| P-08 | **입력 레벨 미터** (마이크 실시간 시각화) | §5 |
| P-09 | 상태 관리 · 옵션 영속화(`localStorage: triptalk_tts`) | §6 |
| P-10 | 환경 점검 — 미지원 브라우저 시 **타이핑 모드 자동 전환** | §7 |
| P-11 | 상황 선택 화면 | §8 |
| P-12 | 대화 진행 로직 · 진행률 바 | §9 |
| P-13 | **받아쓰기 확인 UI** — 사용자가 직접 수정 + 대안 3개 + 신뢰도 표시 | §10 |
| P-14 | **채점 · 피드백** — 발음 교정 + 표현 교정 결과 렌더 | §11 |
| P-15 | **사이드 패널 설정** — 속도, 한국어 표시, 자동재생, 보이스 선택, 클라우드 TTS 키 입력·테스트 | §12 |
| P-16 | 전역 클릭 핸들러 — 🔊 듣기 / 🎯 따라 말하기 / 해석 토글 | §13 |
| P-17 | **마무리 리포트** — 점수 / 턴 수 / 단어 수 / 교정 목록 + 다시하기 | §14 |
| P-18 | 상단 바 — 상황 바꾸기 / 리포트 열기 / 로그인 링크 | |
| P-19 | 힌트 영역 — 모범 표현 · 한국어 해석 토글 | |

### 2.3 로그인 — `login.html` + `login.js`(357줄) + `login.css`(272줄)

| # | 기능 |
|---|---|
| A-01 | 카카오 / 네이버 / Google 소셜 버튼 (브랜드 가이드 색상 + 공식 로고 SVG) |
| A-02 | OAuth 인가 리다이렉트 (`response_type=code`) |
| A-03 | **state 기반 CSRF 방지** + 콜백 후 주소창 인가코드 제거 |
| A-04 | 콜백 처리 → `POST {apiBase}/{provider}` 토큰 교환 |
| A-05 | **데모 모드 폴백** (키 미설정 시 흐름만 시연) |
| A-06 | 이메일 로그인 폼 — 형식 검증 · 비밀번호 표시 토글 · 로그인 유지 |
| A-07 | 세션 저장/복원/삭제 (`localStorage` / `sessionStorage`) |
| A-08 | 로그인 완료 카드 (프로필 · 로그인 방식 · 시각) + 로그아웃 |
| A-09 | 토스트 알림 |
| A-10 | 좌측 브랜드 패널 (그라데이션 · 후기 · 반응형 붕괴) |

### 2.4 공통

- 디자인 토큰 (`:root` 변수 20여 개), 리셋, `.btn` 계열, `.container`, `.badge`, `.logo`
- 반응형 브레이크포인트 (900px / 420px 등)
- `prefers-reduced-motion` 대응
- iOS 입력 확대 방지 (`font-size: max(16px, 1rem)`)

---

## 3. 기술 스택 결정

| 항목 | 결정 | 근거 |
|---|---|---|
| 프레임워크 | **Next.js 16.3.1 · App Router** | 최신 안정 버전(2026-08-19 확인). Route Handlers로 OAuth 서버 확보 |
| React | **19.x** | Next 16 요구사항 |
| Node | **20.9 이상** (로컬 v24.19.0) | Next 16 `engines` 요구사항 |
| 언어 | **TypeScript** | 시나리오·교정 규칙 등 데이터 구조가 복잡해 타입이 큰 이득 |
| 스타일 | **기존 CSS 그대로 이식 → 이후 선택적 모듈화** | 아래 참조 |
| 상태 관리 | **React 내장 (useState / useReducer / Context)** | 외부 라이브러리 불필요한 규모 |
| 배포 | **Vercel** (신규 프로젝트) | 검증 후 기존 `triptalk` 배포를 대체 |

### 스타일 전략 — Tailwind를 쓰지 않는 이유

기존 CSS 1,321줄은 디자인 토큰과 커스텀 애니메이션이 촘촘히 짜인 **완성된 디자인 시스템**이다. Tailwind로 바꾸는 것은 이전이 아니라 재작성이며, "기능 손실 없이"라는 목표와 정면으로 충돌한다.

- **1단계**: `style.css` → `app/globals.css`로 통째 이식, `practice.css` / `login.css`는 해당 레이아웃에서 import. 클래스명 그대로 → **이식 리스크 0**
- **2단계(선택)**: 동작 검증이 끝난 뒤 페이지별 CSS Modules로 점진 분리

### Next.js로 얻는 것 — 소셜 로그인 완성

현재 정적 사이트의 최대 제약은 **클라이언트 시크릿을 둘 서버가 없어 실제 OAuth를 완성할 수 없다**는 점이었다. Next.js Route Handlers(`app/api/auth/[provider]/route.ts`)가 이 문제를 그대로 해결한다. 이전의 가장 큰 실익이다.

---

## 4. 프로젝트 구조

> 2026-08-19 갱신 — 애플리케이션 코드는 `src/` 로 묶었다. 설정 파일과 `public/` 은 루트에 남는다.
> `@/*` 별칭은 `src/*` 를 가리킨다.

```
triptalk-next/
├─ src/
│  ├─ app/
│  │  ├─ layout.tsx                 # 루트 레이아웃 · globals.css · 메타데이터
│  │  ├─ page.tsx                   # 랜딩
│  │  ├─ globals.css                # ← style.css (토큰 + 공통 + 랜딩)
│  │  ├─ practice/                  # practice.css + 연습실 페이지
│  │  ├─ login/                     # login.css + 로그인 페이지
│  │  └─ api/
│  │     ├─ auth/[provider]/route.ts   # OAuth 토큰 교환 (신규)
│  │     └─ tts/route.ts               # 서버 TTS (신규)
│  │
│  ├─ components/
│  │  ├─ landing/                   # Header, Hero, Characters, Situations,
│  │  │                             # Demo, Pricing, Faq, Cta, Footer, ToTop
│  │  ├─ practice/                  # SetupScreen, CharacterStage, MicBar,
│  │  │                             # ConfirmBar, FeedbackStrip, SidePanel, Report
│  │  ├─ login/                     # LoginApp, SocialIcons
│  │  └─ ui/                        # Button, Badge, Logo, Container
│  │
│  ├─ lib/
│  │  ├─ data/                      # characters.ts, situations.ts, faqs.ts,
│  │  │                             # demo.ts, hero.ts, scenarios.ts
│  │  ├─ speech/                    # engine.ts, useSpeechRecognition.ts,
│  │  │                             # useMicLevel.ts, ttsStore.ts
│  │  ├─ correction/                # pronunciation.ts, expression.ts, textDiff.ts
│  │  ├─ hooks/                     # useReducedMotion, useCountUp, useEnvSupport
│  │  └─ auth/                      # session.ts, providers.ts
│  │
│  └─ types/                        # landing.ts, practice.ts, session.ts
│
└─ public/                          # 파비콘 등 (루트 유지)
```

---

## 5. 단계별 작업 계획

### Phase 0 — 스캐폴딩 (0.5일) ✅ **완료 (2026-08-19)**

- [x] `npx create-next-app@latest` → **Next 16.3.1** (TypeScript, App Router, ESLint, src 미사용, Tailwind 제외)
- [x] Node 확인 — v24.19.0 (요구 20.9+ 충족), React 19.2.8
- [x] **별도 저장소 `triptalk-next` 생성 (Private)** — 기존 `triptalk`은 손대지 않아 현재 사이트가 계속 살아 있음
- [x] `npm run build` 통과 확인
- [ ] 루트 레이아웃 · 메타데이터 · 파비콘 이식 → **Phase 1로 이월**
- **완료 기준**: `npm run build` 성공 ✅

### Phase 1 — 디자인 시스템 이식 (0.5일) ✅ **완료 (2026-08-19)**

- [x] `style.css` → `app/globals.css` 이식, `layout.tsx`에서 import
- [x] `practice.css` / `login.css` → 각 하위 레이아웃에서 import
- [x] 공통 UI 컴포넌트 추출: `Button`, `Logo`, `Badge`, `Container`
- [x] 루트 레이아웃 · 메타데이터 · 파비콘 이식 (Phase 0에서 이월)
- **완료 기준**: 토큰·버튼·타이포가 기존과 동일하게 렌더 ✅ (캡처 대조 통과)

### Phase 2 — 데이터 레이어 (1일) ✅ **완료 (2026-08-19)**

- [x] `types/` 에 타입 정의 — `landing.ts` / `practice.ts` / `session.ts`
- [x] `script.js`의 `CHARACTERS` / `SITUATIONS` / `FAQS` / `DEMO` → `lib/data/*.ts`
- [x] `practice.js` §1 시나리오 5종 → `lib/data/scenarios.ts`
- [x] `practice.js` §2 발음 사전, §3 표현 규칙 → `lib/correction/*.ts`
- [x] `practice.js` §4 텍스트 비교 유틸 → `lib/correction/textDiff.ts`
- [ ] textDiff **단위 테스트** — 테스트 러너 미도입으로 보류 (10장으로 이월)
- **완료 기준**: 데이터가 타입 체크를 통과하고 원본과 1:1 일치 ✅
  (캐릭터 8 · 상황 5탭×6 · FAQ 6 · 데모 3×3턴 · 시나리오 5×5턴 개수 대조)

### Phase 3 — 랜딩 페이지 (2일) ✅ **완료 (2026-08-19)**

- [x] 섹션별 컴포넌트 분리 (L-01 ~ L-19)
- [x] 정적 섹션은 **서버 컴포넌트**로 (문제제기·특징·이용방법·후기·푸터·히어로 카피)
- [x] 인터랙션 섹션만 `'use client'` (헤더·히어로채팅·카운트업·캐릭터·탭·데모·요금제·FAQ·CTA·맨위로)
- [x] `IntersectionObserver` 기반 reveal / 카운트업 → `RevealObserver`, `useCountUp`
- **완료 기준**: 인벤토리 L-01~L-19 전부 동작 ✅, 기존 페이지와 캡처 비교 통과 ✅

### Phase 4 — 음성 엔진 (2일) ⚠️ 최난이도 ✅ **완료 (2026-08-19)**

- [x] `lib/speech/engine.ts` — 보이스 비동기 로딩(`voiceschanged`), BAD/GOOD_VOICE 필터, 속도·피치,
      Chrome `cancel()→speak()` 버그 우회와 keep-alive 까지 그대로 이식
- [x] `useSpeechRecognition` — interim 결과, 대안 3개, 시작/중지, 에러 메시지 매핑
- [x] `useMicLevel` — AudioContext 기반 레벨 미터, **언마운트 시 트랙·컨텍스트 해제**
- [x] 클라우드 TTS — OpenAI / ElevenLabs 호출 + blob 캐시 + 실패 시 내장 음성 폴백 (engine 내부)
- [x] **SSR 가드**: 엔진은 `getEngine()` 지연 생성, 브라우저 판정은 `useSyncExternalStore`
      (서버 스냅숏 `null`) 로 처리해 hydration 불일치 0
- **완료 기준**: 연습실에서 5개 시나리오 재생·인식·미터 동작 확인 ✅

### Phase 5 — 연습실 (3일) ⚠️ 최난이도 ✅ **완료 (2026-08-19)**

- [x] 상태 모델 재설계 — `setup → stage(대화 → 받아쓰기 확인 → 피드백) → report`
      전이를 `PracticeApp` 의 파생 상태로 정리 (메시지·카드는 배열 렌더)
- [x] 화면 컴포넌트 분리 (P-11 ~ P-19) — TopBar / SetupScreen / ChatView / Hint /
      ConfirmBar / MicBar / SidePanel / ReportModal
- [x] 채점·피드백 로직 이식 (P-14) — 발음 점수, 발음 교정, 표현 교정, 칭찬, 핵심 누락, 따라 말하기
- [x] 사이드 패널 · 옵션 영속화 (P-15, P-09) — `lib/speech/ttsStore.ts`
- [x] 미지원 브라우저 타이핑 모드 (P-10)
- [x] 마무리 리포트 (P-17)
- [x] 전역 클릭 위임(§13) → `ToolsContext` + `SpeakButton` / `ShadowButton` 로 대체
- **완료 기준**: 5턴 시나리오 완주 → 리포트 수치 일치 확인 ✅

### Phase 6 — 로그인 + 실제 OAuth (1.5일) ✅ **코드 완료 (2026-08-19)**

- [x] 로그인 UI 이식 (A-01, A-06, A-08, A-09, A-10)
- [x] 세션 유틸 `lib/auth/session.ts` (A-07)
- [x] **Route Handler 신규 구현** `app/api/auth/[provider]/route.ts`
      — 인가코드 → 토큰 교환 → 프로필 조회 → 정규화 응답 (카카오/네이버/Google)
- [x] 환경변수 정리 — `.env.example` 참고.
      클라이언트 ID 는 `NEXT_PUBLIC_*`, 시크릿은 서버 전용
- [ ] 각 개발자 콘솔에 Redirect URI(`/login`) 등록 — **외부 작업, 키 발급 후 진행**
- [x] 데모 모드 폴백 유지 (A-05) — 키가 없으면 안내 문구와 함께 흐름만 시연
- **완료 기준**: 키 없을 때 데모 모드 정상 ✅ / 실제 3개 제공자 로그인은 키 발급 후 확인 필요

### Phase 7 — 검증 · 배포 전환 (1일) ✅ **검증 완료 (2026-08-20)**

- [x] 7장 체크리스트 중 **기능·품질·반응형** 항목 통과 (Chromium 기준)
- [x] Chromium / Firefox / WebKit **엔진** 검증 — Playwright 자동 검사.
      3개 엔진 × 3개 화면 × 3개 폭(1280/900/390) = 27조합에서 콘솔 에러·경고 0, 가로 스크롤 0
- [x] Firefox **타이핑 모드 자동 전환** 확인 — 안내 배너 노출 + 타이핑으로 한 턴 완주
- [x] 연습실 5턴 완주 → 리포트 자동 표시 확인 (타이핑 답변은 발음 채점 제외 — 원본 `practice.js:1069` 와 동일)
- [x] Lighthouse 비교 — 아래 표. **접근성·권장사례·SEO 동일 이상, 연습실은 성능이 크게 개선**
- [x] **배포 전 보안 점검** — 9.5장 참고. 발견한 4건 모두 조치
- [ ] iOS Safari **실기기** 확인 (마이크 권한 · 입력 확대) — 자동화로 대체 불가, 기기에서 직접
- [ ] `triptalk-next` 저장소를 Vercel 신규 프로젝트로 연결
- [x] **저장소 공개 여부** — Vercel Hobby 는 *개인 계정* 소유의 Private 저장소를 그대로
      배포한다 (제약은 조직 소유 저장소에만 적용). 배포만 놓고 보면 공개할 이유가 없어
      한동안 Private 으로 두었고, **2026-08-23 과제 제출을 위해 Public 으로 전환했다**
- [ ] 검증 후 기존 `triptalk` 배포를 대체 — 도메인 이전 또는 기존 프로젝트 정리
- [x] ~~GitHub Pages 처리 결정~~ — **중단 완료(2026-08-19).** 배포처는 Vercel 단독

**Lighthouse (프로덕션 빌드 · 헤드리스 Chrome · 원본은 `python3 -m http.server` 로 구동)**

| 화면 | 성능 | 접근성 | 권장사례 | SEO |
|---|---|---|---|---|
| 랜딩 — 새 / 원본 | 99 / 100 | 95 / 95 | 100 / 100 | 100 / 100 |
| 연습실 — 새 / 원본 | **98 / 86** | **96 / 94** | 100 / 100 | 100 / 100 |
| 로그인 — 새 / 원본 | 99 / 100 | 96 / 96 | 100 / 100 | 60 / 60 |

성능 1점 차이는 React 하이드레이션 비용이고, 연습실은 오히려 12점 올랐다.
로그인의 SEO 60 은 원본과 같은 값이라 회귀가 아니다 (로그인 화면에 메타 설명이 없어서 나는 감점).

**총 예상: 11.5일** (1인 기준, 검증 포함)
**실제: Phase 0~7 완료 (2026-08-19 ~ 08-20).** 남은 것은 iOS 실기기 확인과 Vercel 프로젝트 연결뿐이다.

---

## 5.1 이전 중 내린 판단 (원본과 다른 점)

무손실이 원칙이지만, Next.js/React 로 옮기면서 **불가피하거나 명백히 이득인** 결정이 몇 개 있었다.
화면과 동작은 모두 동일하다.

| # | 원본 | 새 코드 | 이유 |
|---|---|---|---|
| 1 | `<body class="practice-body">` / `auth-body` | `body:has(.practice-page)` / `body:has(.auth)` | App Router 는 루트 레이아웃이 `<body>` 를 소유한다. `:has()` 로 바꾸면 JS 없이 SSR 시점부터 배경색이 맞아 깜빡임이 없다. 두 클래스가 하던 일은 `background` 하나뿐이다. |
| 2 | `.html` 링크 (`practice.html`) | 라우트 경로 (`/practice`) | App Router 규칙 |
| 3 | `login.js` 의 `AUTH_CONFIG.clientId` 하드코딩 | `NEXT_PUBLIC_*` 환경변수 | 키를 코드에 두지 않기 위해. 비어 있으면 원본과 똑같이 데모 모드 |
| 4 | 마이크 스트림·AudioContext 를 해제하지 않음 | 언마운트 시 트랙 stop + context close | 9장 리스크표의 "마이크·오디오 리소스 누수" 대응 |
| 5 | 마이크 버튼이 `synth.cancel()` 만 호출 | `engine.cancel()` (클라우드 오디오도 정지) | 원본 주석이 명시한 의도("말하기 버튼을 누르면 재생 중인 음성은 자동으로 멈춥니다")를 클라우드 음성에도 적용 |
| 6 | 로컬 서버 안내 경고 배너 | HTTPS/localhost 안내로 문구 변경 | `python3 -m http.server` 안내가 Next 프로젝트에서는 맞지 않는다 |
| 7 | 채팅 속 "해석 보기" 토글이 설정 화면 체크박스와 따로 놈 | 같은 상태를 공유 | React 상태 일원화. 사용자가 보는 결과는 항상 일치 |

**그대로 남긴 원본 동작 하나** — `🎯 따라 말하기` 를 누르면 원본은 "목표 문장: …" 을 잠깐 넣었다가
곧바로 `setMicEnabled(true)` 가 기본 안내로 덮어쓴다. 호출 순서를 그대로 옮겨 화면 결과도 동일하다.
(`components/practice/PracticeApp.tsx` 의 `shadow()` 주석 참고)

---

## 6. 핵심 기술 과제

### 6.1 Web Speech API와 SSR — 가장 큰 함정

`window.SpeechRecognition`, `window.speechSynthesis`, `AudioContext`는 **서버에 존재하지 않는다.** 서버 렌더링 중 접근하면 즉시 크래시한다.

**대응**
- 음성을 쓰는 컴포넌트는 전부 `'use client'`
- API 접근은 반드시 `useEffect` 내부에서 (렌더 중 접근 금지)
- 지원 여부 상태는 초기값 `null`(판정 전) → 마운트 후 확정. **`false`로 시작하면 hydration 불일치 발생**
- `speechSynthesis.getVoices()`는 비동기 → `voiceschanged` 이벤트 구독 필수

### 6.2 명령형 → 선언형 재설계

`practice.js`는 DOM을 직접 조작하는 명령형 코드다. React로 옮길 때 **1:1 번역이 아니라 상태 모델 재설계**가 필요하다.

- 화면 전이를 `useReducer` 액션으로 정리
- `innerHTML` 조립 → 컴포넌트 + 배열 렌더
- 전역 클릭 위임(§13) → 각 컴포넌트의 onClick
- **가장 버그가 나기 쉬운 구간이므로 Phase 5에 충분한 시간을 배정**

### 6.3 정리(cleanup) 누락

`AudioContext`, `MediaStream`, `SpeechRecognition`, `URL.createObjectURL`은 언마운트 시 반드시 해제해야 한다. 누락 시 마이크가 계속 켜져 있거나 메모리가 샌다. 모든 훅에 `useEffect` cleanup을 강제한다.

### 6.4 localStorage 접근

`localStorage`도 서버에 없다. 읽기는 `useEffect`에서, 초기 렌더는 기본값으로 처리한다.

---

## 7. 완료 검증 체크리스트

**기능** — 2장 인벤토리의 L-01~19, P-01~19, A-01~10 **전 항목** 수동 확인

**브라우저** (2026-08-20 · Playwright 자동 검사)
- [x] Chrome / Chromium (음성 인식 API 감지됨)
- [x] WebKit (Safari 엔진 — 음성 인식 API 감지됨)
- [x] Firefox (**타이핑 모드로 자동 전환** 확인 — 배너 노출 + 타이핑 한 턴 완주)
- [ ] iOS Safari (마이크 권한 · 입력 확대 없음) — **실기기 필요, 자동화 불가**

**반응형**
- [x] 1280px / 900px / 390px 에서 레이아웃 붕괴 없음 (3엔진 모두 가로 스크롤 0px)

**품질**
- [x] 콘솔 에러 0 (27조합 + 5턴 완주 시나리오)
- [x] hydration 경고 0
- [x] `npm run build` 경고 없이 성공
- [x] 마이크가 사용 후 확실히 해제되는지 — `useMicLevel` 이 언마운트 시 `getTracks().stop()`,
      `AudioContext.close()`, `cancelAnimationFrame` 을 모두 호출하고 `useSpeechRecognition` 은
      `recognition.stop()`, 엔진은 `URL.revokeObjectURL()` 로 정리한다

**회귀 비교**
- [x] 기존 사이트와 새 사이트를 나란히 띄우고 Lighthouse 4개 지표 비교 (Phase 7 표)

---

## 8. 배포 전환

1. `triptalk-next`를 Vercel 신규 프로젝트로 연결 → **프리뷰 배포**로 먼저 검증
2. 환경변수 등록 (OAuth 시크릿 6종)
3. 각 OAuth 콘솔의 Redirect URI를 새 도메인으로 갱신
4. 검증 통과 후 프로덕션 전환 — 기존 `triptalk-chi.vercel.app` 대체
5. 구 저장소 `triptalk`는 아카이브 또는 참조용 보존 (삭제하지 않는다)
5. ~~GitHub Pages 중단~~ — **2026-08-19 완료.** Route Handler(소셜 로그인)와 정적 export가 양립하지 않아 Vercel 단일 배포로 정리함

---

## 9. 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| `practice.js` 상태 기계 재설계에서 미묘한 동작 차이 발생 | 높음 | Phase 5에 3일 배정, 시나리오 5종 전부 완주 테스트 |
| Web Speech API SSR 크래시 / hydration 불일치 | 높음 | 6.1 지침 준수, 훅 단위 선검증(Phase 4) |
| 마이크·오디오 리소스 누수 | 중간 | 모든 훅 cleanup 강제, 탭 표시로 육안 확인 |
| CSS 이식 중 레이아웃 미세 깨짐 | 중간 | 클래스명 유지 전략, 캡처 비교 |
| OAuth 콘솔 설정(키 발급·Redirect URI)이 외부 의존 | 중간 | 데모 모드 폴백을 유지해 키 없이도 진행 가능하게 |
| 일정 초과 | 중간 | Phase 6(로그인)은 분리 가능 — 최악의 경우 후속 작업으로 미룸 |

---

## 9.5 이전 이후 변경 이력

무손실 이전(Phase 0~6)이 끝난 뒤 요청으로 반영한 기능 변경을 여기에 남긴다.
2장 인벤토리는 "이전 시점의 원본 기준"이므로 수정하지 않는다.

### 2026-08-23 · 연습실 음성을 OpenAI 로, 단 런타임 호출은 0

**문제.** 브라우저 내장 음성은 기기에 깔린 음성에 좌우된다. 확인해 보니 작업용
맥에는 쓸 만한 영어 음성이 `Samantha` 하나뿐이었다. 심사위원 기기에서 어떻게
들릴지 통제할 수 없다는 뜻이다.

**그런데 매번 API 를 부를 이유가 없었다.** 연습실 대본은 `scenarios.ts` 에
고정돼 있다. 말할 문장이 미리 정해져 있는데 방문자마다 같은 문장을 같은
목소리로 다시 만들면 돈만 나간다. 세어 보니 **254개 · 9,342자**가 전부였다.

그래서 한 번 굽고 끝내는 쪽으로 갔다 (`scripts/prerender-tts.mts`).

| | 매번 호출 | 미리 굽기 |
|---|---|---|
| 런타임 OpenAI 호출 | 재생마다 | **0회** |
| 트래픽이 늘면 | 청구액도 늘어남 | 그대로 |
| 서버에 키 | 필요 | **불필요** |
| 재생 지연 | 생성 대기 | 정적 파일 |

**굽는 대상은 세 갈래다.** `turns[].ai`(캐릭터 대사), `turns[].models[][0]`
(섀도잉이 캐릭터 목소리로 읽는 모범 답안), 설정 화면의 `SAMPLE`.
시나리오마다 `voice.oa`(음성)와 `voice.inst`(톤 지시문)가 따로 있어서
프리렌더해도 캐릭터별 연기가 그대로 살아남는다.

**재생 순서**에 한 단계를 끼워 넣었다 (`engine.ts`).
개인 키 → **구워둔 파일** → 서버 TTS → 브라우저 내장.
매니페스트에 없는 문장은 예전 경로로 내려가므로, 대본을 늘려도 즉시 깨지지 않는다.

**검증.** dev 서버 네트워크 로그에 `GET /tts/*.mp3` 만 찍히고 `POST /api/tts`
는 한 번도 없었다. 결과물은 254개 · 13MB, 실패 0.

**비용 방어.** OpenAI 크레딧 $5 선불, **auto-reload 끔**. 잔액이 0 이 되면
요청이 멈출 뿐 자동 결제되지 않는다. `api/tts/route.ts` 의 IP 별 상한은
인스턴스 메모리 기반이라 서버리스에서 진짜 전역 상한이 아니었는데,
런타임 호출 자체가 0 이 되면서 이 한계가 무의미해졌다.

**남은 것** — 프로덕션 Vercel 에는 `OPENAI_API_KEY` 를 넣지 않는다. 넣을
이유가 없고, 넣지 않는 편이 유출·초과청구 양쪽에서 안전하다. 실시간 AI 대화를
붙이게 되면 그때 다시 판단한다.


### 2026-08-21 · 랜딩 통계 실제화, 첫 배포, 💳 돈 트랙 신설

**세 가지를 했다.**

**1. 랜딩 통계를 손으로 적은 값에서 계산값으로.** `누적 학습자 38만명` 은 근거가
없었다. 출시 전이라 근거로 삼을 실사용 데이터가 없는데 시안용 숫자가 그대로 남아 있었다. `62 학습 상황`,
`1,200 핵심 표현` 도 마찬가지였다 — 전부 합쳐도 110개 남짓이다.

넷을 실제 데이터에서 세도록 바꿨다 (`lib/data/stats.ts`).

| 이전 (손으로 적음) | 지금 (코드가 셈) |
|---|---|
| 62 학습 상황 | 연습 시나리오 (`SCENES`) |
| 8 AI 캐릭터 | AI 캐릭터 (`CHARACTERS`) |
| 1,200 핵심 표현 | 모범 답안 문장 (`turns[].models`) |
| 38 누적 학습자(만명) | 교정 규칙 (`RULES`+`SILENT`+`STRESS`+`PATTERNS`) |

애초에 숫자가 어긋난 이유가 **손으로 적어 두고 아무도 안 고쳤기 때문**이라,
같은 일이 다시 생기지 않게 계산으로 바꿨다. 내용을 추가하면 숫자가 따라 올라간다.
실제로 돈 트랙 5개를 넣자 `5 → 10`, `50 → 100` 으로 저절로 바뀌었다.

**`stats.ts` 를 따로 둔 이유.** `hero.ts` 는 `HeroChat`·`Stats` 두 **클라이언트**
컴포넌트가 쓴다. 계산을 거기 두면 시나리오와 교정 사전이 통째로 브라우저 번들에
실린다. 서버 컴포넌트인 `Hero` 에서 세어 `Stats` 에 props 로 내려보낸다.

**2. 첫 배포.** `AUTH_SECRET` 을 생성해 Vercel 에 넣고 프로덕션으로 올렸다.
→ https://triptalk-next-mynode.vercel.app

- `triptalk-next.vercel.app` 은 **다른 사용자가 이미 선점**해 `-mynode` 가 붙었다
- 기본으로 **배포 보호(SSO)** 가 걸려 있어 링크 공유가 안 됐다. 해제했다
- 중복 Vercel 프로젝트 3개(`triptalk`·`triptalk-waze`·`triptalk-742r`)와
  머지 완료된 브랜치 3개를 정리했다. 커스텀 도메인은 원래 0개였다

**3. 💳 돈 트랙 5개 신설** — 카드 결제 거절 · 해외 ATM 인출 실패 · 환전소 ·
택스 리펀드 · 카드 분실 신고. 배경은 11장에 있다.

**남은 것** — `noindex` 미적용. 상황 카드 30장 중 연습 가능한 것은 10개인데
화면상 구분이 없다. AI 대화 연동·회원 DB·결제는 다음 단계 과제다 (10장).

> 2026-08-29 갱신 — 카드 구분은 해소했다. 연습할 수 있는 카드는 눌러서 바로 들어가고,
> 아직 없는 카드는 지우지 않고 "준비 중" 으로 둔다. `noindex` 는 여전히 미적용.


### 2026-08-19 · 연습실을 캐릭터 1:1 대화 화면으로 (P-12 / P-16 / P-19 변경)

**요청** — 대화 연습이 텍스트 위주라 캐릭터와 1:1로 말하는 느낌으로 바꾸고 싶다.
캐릭터가 말할 때 영어 자막은 사용자가 선택했을 때만 보이게 한다. **피드백은 그대로 유지.**

| 항목 | 이전 | 지금 |
|---|---|---|
| 대화 화면 | 채팅 로그 (`.talk__chat`) | 캐릭터 무대 (`CharacterStage`) — 큰 아바타 + 지금 대사 하나 |
| 영어 자막 | 항상 인쇄 | **기본 꺼짐.** `👁 영어 자막 보기` 로 이번 턴만 펼치거나, 설정의 `영어 자막 항상 보기` 로 고정 |
| 한글 해석 | 토글 (기본 켜짐) | 토글 (**기본 꺼짐**) |
| 지금 대사 | 로그 안에서 스크롤됨 | `position: sticky` 로 상단 고정 — 피드백을 읽는 동안에도 계속 보임 |
| 내 답변 + 피드백 | 로그에 계속 쌓임 | 직전 것을 `FeedbackStrip` 으로 무대 아래 표시. **카드 구성·문구·판정 로직은 그대로** |
| 지난 대화 | 항상 펼쳐진 로그 | `TranscriptLog` 접이식 (`지난 대화 · 피드백 n`) — 내용은 기존 `ChatView` 그대로 |
| 재생 표시 | 없음 | 음성 재생 중 아바타에 링이 퍼짐 (`is-speaking`) |

**바꾸지 않은 것** — 채점 공식, 발음/표현 교정 규칙, 카드 문구, 사이드 패널, 마무리 리포트,
힌트·받아쓰기 확인·마이크 바, 턴 사이 대기 시간(900ms/700ms).

**영어 자막과 한글 해석 모두 기본으로 꺼져 있다.** 캐릭터의 말은 먼저 귀로 듣고,
못 알아들었을 때만 `👁 영어 자막 보기` 또는 `해석 보기` 로 확인한다.
무엇을 말해야 하는지는 아래 힌트 영역(`💬 이렇게 답해보세요`)이 한국어로 계속 안내하므로
자막이 없어도 대화는 막히지 않는다.

### 2026-08-19 · 요금 정책 문구 조정

- 무료 체험 **7일 → 3일**. Free 요금제가 이미 "기본 상황 5개 · 하루 대화 3회"를 영구 무료로
  주고 있어 맛보기 역할은 Free 가 맡는다. 체험은 Traveler 프리미엄 기능을 잠깐 여는 성격이라
  길 이유가 없고, 3일이면 복기 리포트가 쌓이는 루프를 한 바퀴 돌 수 있다.
  (`Pricing`, `Cta`, `LoginApp` 배지, FAQ 답변 — 총 6곳)
- **결제 후 7일 이내 환불 조항은 유지.** 체험이 짧아진 만큼 결제 직후 이탈 경로로서 더 중요해졌다.
- 향후 검토 — 기간 대신 **횟수 기반**("상황 3개 무료")이 이 제품에 더 맞을 수 있다.
  가입만 하고 방치해 체험을 날리는 일이 없고 콘텐츠 소진량도 정확히 통제된다.

### 2026-08-20 · 마이크 고지와 CSP

**질문** — 사용자 데이터베이스 서버는 안전한가. 확인해 보니 **데이터베이스가 없다.**
의존성은 `next` · `react` · `react-dom` · `server-only` 뿐이고 ORM·연결 문자열·파일 I/O 가 하나도 없다.
서버는 요청을 처리하고 아무것도 저장하지 않는다. 대신 **데이터가 흐르는 곳**을 전수 추적했다.

| 데이터 | 어디에 남나 | 우리 서버 저장 |
|---|---|---|
| OAuth 프로필(이름·이메일) | 서명된 쿠키 — 사용자 브라우저 | 안 함 |
| 사용자가 말한 문장(텍스트) | 브라우저 메모리. 채점도 브라우저 안 | **서버로 가지 않음** |
| 음성 설정 + 개인 API 키 | `localStorage: triptalk_tts` (평문) | 안 함 |
| 마이크 오디오 | **브라우저 벤더 서버** (Chrome→Google, Safari→Apple) | 우리 서버를 거치지 않음 |
| 시나리오 대사 | `/api/tts` → OpenAI | 안 함 (스트림 통과) |

`speak()` 호출부를 전수 확인해 **사용자 발화가 외부 TTS 로 가는 경로가 없음**을 확인했다.

여기서 두 가지를 보강했다.

**1. 마이크·음성 처리 고지 (`SetupScreen`)** — 음성 인식은 브라우저 기능이고 브라우저는
오디오를 자사 서버로 보낸다. 우리가 만든 동작은 아니지만 사용자 목소리가 나가는 일이라
마이크를 켜기 전 화면에서 밝힌다. 타이핑으로도 연습할 수 있다는 선택지를 함께 둔다.

**2. CSP** — 노린 것은 XSS 차단 자체가 아니라 **개인 API 키의 유출 경로를 닫는 것**이다.
키는 localStorage 에 평문이라 스크립트 주입이 성공하면 읽히지만, 나갈 길이 없으면 피해가 크게 준다.

| 시도 (키를 읽었다고 가정) | 결과 |
|---|---|
| `fetch` 로 공격자 서버에 POST | 막힘 |
| 이미지 URL 에 실어 보내기 | 막힘 |
| 외부 스크립트 불러오기 | 막힘 |
| WebSocket 으로 빼내기 | 막힘 |
| 허용된 곳(OpenAI)으로 | 정상 통과 |

**`script-src` 에 `'unsafe-inline'` 을 남긴 이유.** Next 는 하이드레이션 데이터를 인라인
`<script>` 로 넣는다. nonce 로 허용하려면 요청마다 nonce 를 만들어야 하고 그러면 **세 페이지가
모두 동적 렌더링이 된다.** 지금 정적 프리렌더로 Lighthouse 98~99 인데 그걸 내주면서 얻는 값이
크지 않다고 봤다. 지켜야 할 사용자 데이터가 서버에 생기면 그때 nonce 방식으로 올린다.

**`upgrade-insecure-requests` 는 뺐다.** WebKit 이 이 지시어를 만나면 `http://localhost` 의
하위 리소스까지 https 로 올리려다 TLS 오류로 화면을 깨뜨린다 (Chromium 은 localhost 예외).
`npm start` 를 사파리로 여는 것이 불가능해지는데, 정책이 이미 자기 출처와 https 호스트 둘로
범위를 못박아 http 리소스가 끼어들 자리가 없어 실익이 없다. 대신 **HSTS** 를 넣었다
(`max-age=63072000`, `includeSubDomains`·`preload` 없이 — 되돌리기 어려운 선택은 피한다).

**검증** — 3엔진 27조합 CSP 위반 0건, 로그인·연습실 회귀 없음.

**남은 것** — 이용약관·개인정보처리방침 링크가 아직 `noop` 이다. 실제 공개 서비스라면
마이크 오디오의 외부 전송을 포함한 실제 문서가 필요하다 (10장).

> 2026-08-28 갱신 — 두 문서를 실제로 작성했다. `/terms` · `/privacy` 이며 마이크
> 오디오가 브라우저 벤더 서버로 전송된다는 사실을 포함한다. 9.6 참고.

### 2026-08-20 · 로그인을 서버 세션으로 완성, 이메일 폼 제거

**질문** — 회원가입과 로그인이 제대로 되는가. 확인해 보니 **되지 않았다.**

원본 `login.js:286-307` 부터 이메일 로그인은 인증이 아니라 **형식 검사**였다.
이식은 충실했지만(회귀 아님), 그대로 공개하면 문제가 된다. 실측한 것:

| 시도 | 이전 결과 |
|---|---|
| 남의 이메일 + 아무 비밀번호 8자 | 로그인 통과. 비밀번호를 어디와도 대조하지 않음 |
| localStorage 에 세션을 직접 써넣고 새로고침 | 로그인 상태로 표시. 서버 검증 없음 |
| "회원가입" 링크 | 토스트만 뜨고 가입 기능 자체가 없음 |

권한 상승 피해는 없었다 — 세션을 읽는 곳이 로그인 페이지 자신뿐이고 지키는 자원이 없다.
**진짜 문제는 두 가지였다.** 사용자에게 실제 비밀번호를 입력하게 유도한다는 것
(`autoComplete="current-password"` 까지 걸려 있었다), 그리고 **소셜 키를 하나라도 넣으면
`anyConfigured()` 가 참이 되어 "데모 모드" 안내가 사라진다**는 것. 배포 직전의 상태가
정확히 그랬다. 동작하지 않는 이메일 로그인이 진짜처럼 보이는 채로 공개될 뻔했다.

**조치 — 소셜 로그인만 진짜로 만들고, 이메일/비밀번호는 없앤다.**

| 항목 | 이전 | 지금 |
|---|---|---|
| 세션 보관 | 클라이언트 `localStorage` / `sessionStorage` | **서버가 HMAC-SHA256 으로 서명한 HttpOnly 쿠키** (`lib/auth/cookie.ts`, 7일) |
| 세션 조회 | 저장소에서 읽음 | `GET /api/auth/session` — 서명이 맞지 않거나 만료면 `null` |
| 로그아웃 | 저장소 삭제 | `DELETE /api/auth/session` — 쿠키는 서버만 지울 수 있다 |
| 이메일 로그인 | 형식 검사 후 통과 | **폼 제거.** 소셜로 시작하면 그 자리에서 가입까지 끝난다고 안내 |
| 데모 세션 | 클라이언트가 직접 생성 | `POST /api/auth/demo` — **해당 제공자의 실제 키가 있으면 409** 로 거절 |
| 데모 표시 | 키가 하나도 없을 때만 안내 | 키 없는 서비스는 **버튼 라벨에 "(데모)"**, 안내도 해당 서비스를 이름으로 밝힘 |

**검증** (프로덕션 빌드)

- 쿠키 속성 — `Secure; HttpOnly; SameSite=lax`. 자바스크립트로 `document.cookie` 를 읽어도 보이지 않는다
- `localStorage` 에 세션을 주입해도 **무시된다** (저장소에 남는 키 자체가 없다)
- 쿠키 서명 4바이트를 바꾸면 **로그아웃 상태로 떨어진다**
- 실제 키가 설정된 제공자를 데모로 요청 → **409**
- `AUTH_SECRET` 없이 실제 로그인 시도 → **500 + 로그**. 조용히 반쯤 동작하게 두지 않는다

**`SameSite=lax` 인 이유.** OAuth 제공자에서 되돌아오는 이동은 top-level GET 이라
`strict` 면 쿠키가 붙지 않아 로그인 직후 세션이 없는 것처럼 보인다.

**`AUTH_SECRET` 이 배포본에서 필수인 이유.** 없으면 인스턴스마다 임의의 키를 쓰게 되어,
서버리스에서 인스턴스가 바뀔 때마다 로그인이 풀린다. `.env.example` 참고.

**남은 한계 — 아직 "회원 데이터"는 없다.** 세션은 진짜지만 사용자 저장소(DB)가 없어
학습 기록은 여전히 브라우저에만 남는다. 유료 기능처럼 지켜야 할 자원이 생기면
그때 사용자 테이블과 서버 저장이 필요하다 (10장).

### 2026-08-20 · 배포 전 보안 점검과 조치

공개 배포를 앞두고 점검한 결과 4건을 고쳤다. **화면과 동작은 그대로다.**

| # | 문제 | 조치 | 확인 |
|---|---|---|---|
| 1 | `/api/tts` 에 호출 제한이 없어, 서버 키를 쓰는 이 엔드포인트를 아무나 반복 호출하면 OpenAI 비용이 그대로 청구된다 | 같은 출처 요청만 허용 + IP 당 1분 20회 제한 (`route.ts`) | 타 사이트 Origin → 403, 21번째 호출 → 429 |
| 2 | 토큰 교환 실패 시 응답 **본문 전체**를 `console.error` 로 남긴다. 상태가 200 이어도 형식이 다르면 이 경로를 타므로 토큰이 Vercel 로그에 찍힐 수 있다 | 오류 코드만 로깅 | 코드 검토 |
| 3 | 클라이언트가 보낸 `redirectUri` 를 검증 없이 토큰 교환에 사용 | 요청 출처와 다르면 400 (제공자 콘솔 화이트리스트와 이중 방어) | 남의 도메인 → 400, 자기 출처 → 통과 |
| 4 | 보안 응답 헤더가 하나도 없음 | `next.config.ts` 에 CSP `frame-ancestors` · `X-Frame-Options` · `nosniff` · `Referrer-Policy` · `Permissions-Policy` 추가, `poweredByHeader` 해제 | `curl -I` 로 5종 확인 |

`Referrer-Policy: strict-origin-when-cross-origin` 은 특히 중요하다. 로그인 콜백 주소에는
**OAuth 인가 코드가 쿼리로 붙어 있어서**, 이 헤더가 없으면 그 페이지에서 외부로 나가는 요청의
Referer 에 인가 코드가 실려 나갈 수 있다.

`Permissions-Policy` 는 `microphone=(self)` 로 자기 출처만 열어 둔다. 연습실이 마이크를 쓰므로
전부 닫으면 안 된다. 카메라·위치는 쓰지 않으니 닫았다.

**이때 그대로 두었던 것 — 세션을 브라우저 저장소에 두는 방식.** 데모 단계의 설계라고 보고
넘겼으나, 바로 아래 항목에서 다시 들여다본 결과 그대로 공개할 수 없다고 판단해 서버 세션으로
바꿨다. 판단 근거는 다음 절에 있다.

**호출 제한의 한계.** 카운터가 서버 인스턴스 메모리에 있어 서버리스에서는 인스턴스마다 따로 센다.
한 클라이언트의 연속 호출은 확실히 끊지만 전역 상한은 아니다. 트래픽이 늘면 Vercel KV 같은
공유 저장소로 옮겨야 한다.

### 2026-08-19 · 영어 발음 자연스럽게 (P-05 / P-06 보강)

**요청** — 발음이 기계 티가 난다. 더 사람처럼, 미국식으로.

**원인** — 브라우저 내장 음성은 기기에 설치된 음성 품질이 전부다. 확인해 보니 작업 기기에는
애플 **기본** 음성만 있었고(향상됨/프리미엄 미설치) 쓸 만한 미국 음성은 구형 `Samantha` 하나뿐이었다.
코드로는 이 음성을 사람처럼 만들 수 없다.

| 조치 | 내용 |
|---|---|
| 서버 TTS 신설 | `app/api/tts/route.ts` — 서버에 `OPENAI_API_KEY` 하나만 두면 방문자가 각자 키를 넣지 않아도 자연스러운 미국식 발음이 나온다. `GET` 은 가용 여부만(무비용) 알려준다 |
| 재생 우선순위 | 개인 키 → 서버 TTS → 브라우저 내장 음성 순으로 자동 폴백 |
| 미국식 우선 강화 | `rankVoice` 의 지역 일치 가산을 100→140, 타 영어권을 55→40 으로 벌려 미국 상황에서 영국·호주·인도 억양이 뽑히지 않게 함 |
| **한글 macOS 대응** | 고품질 음성 판별에 `향상`·`프리미엄`·`고급` 라벨 추가. 기존 정규식이 영어 단어만 봐서, 한글 macOS 에서 애써 설치한 고품질 음성이 기본 음성과 같은 점수를 받던 문제 |
| 안내 노출 | 기기에 고품질 음성이 하나도 없으면 상황 선택 화면에 안내를 띄운다. 접힌 패널 안에만 있던 설치법을 밖으로 꺼냄 |

**영국식 Aisha(호텔)는 그대로 뒀다.** 시나리오 설명과 카드 배지에 "영국식"이 명시된 의도된 설정이라,
미국식으로 바꾸면 기능이 하나 사라진다. 바꾸려면 `lib/data/scenarios.ts` 의 `hotel.char.lang` 과
`voice.inst`, 설명 문구를 함께 고쳐야 한다.

---

## 9.6 공모전 제출 전 정리 (2026-08-28 ~ 08-30)

신한 Tomorrow Challenge 제출을 앞두고, **화면에 남아 있던 사실이 아닌 것**을 걷어내고
서비스의 성격을 확정한 구간이다. 판단의 축은 하나였다 — 심사위원이 배포된 사이트를
직접 눌러 볼 것이고, 그때 문서와 화면이 어긋나면 그 자체가 감점이다.

### 걷어낸 것

| 무엇 | 왜 |
|---|---|
| 후기 섹션 | 인물도 문장도 지어낸 것이었다 |
| 로그인 화면의 후기 | 같은 이유. "김지현 · 3개월 사용" 은 존재하지 않는 사람이다 |
| 마지막 CTA 의 이메일 폼 | 주소를 받아 놓고 "데모라 발송되지 않습니다" 라고 실토하고 있었다. 쓰지 않을 개인정보는 받지 않는다 |
| 요금제 섹션 전체 | 아래 "성격 확정" 참고 |
| 카카오·네이버·Google 소셜 로그인 | 아래 참고 |

### 성격 확정 — 이것은 돈을 버는 앱이 아니다

TripTalk 은 **슈퍼SOL 로 환전한 고객에게 무료로 제공되는 부가 서비스**로 간다.
사용자에게 구독료를 받지 않고 결제 수단도 요구하지 않는다.

- 선행 확인: 슈퍼SOL 앱인앱 중 **사용자가 이용료를 내는 서비스는 공개 자료상 없다.**
  금융 서비스와 포인트·멤버십 혜택뿐이다. (앱 내부까지 확인한 것은 아니다.)
- 그래서 예정 요금표를 내걸 이유가 사라졌다. 결제도 회원 DB 도 없는 상태에서
  ₩12,900 을 적어 두는 것은 앞서 걷어낸 가짜 후기와 같은 종류의 문장이다.
- 사업성 분석(마진 61% · 허용 CAC · 목표 CPC)은 **B2C 유료화를 가정한 별도 시나리오**로만
  유효하다. 공모전 서술에는 쓰지 않는다. 대신 답해야 할 질문은
  "무료로 주는 데 얼마가 드나" 이고, 근거는 대사 254개 사전 생성 실비 **$0.20(1회성)** 이다.
  프리렌더 구조라 이용자가 늘어도 음성 비용은 늘지 않는다.

### 무료 횟수와 로그인

- 비로그인 방문자에게 **총 3회**를 연다(처음엔 5회였다). 로그인부터 요구하면
  대부분 그 자리에서 돌아가기 때문이다.
- **슈퍼SOL 환전 고객은 여행 날짜까지 하루 5회.** 주소는
  `?from=supersol&until=2026-09-15` 이고 `until` 이 출국 예정일이다.
  - 무기한이 아닌 이유 — 환전에서 비롯된 자격이라 그 여행이 끝나면 근거도 끝난다.
    다음 여행의 환전 때 다시 만나는 편이 맞고, 그래야 재방문 주기를 여행 주기가 정한다.
  - 무제한이 아닌 이유 — 삭제한 요금제 카드는 유료 등급을 "대화 무제한" 으로 적었지만
    사업 분석의 실제 설계는 **하루 5회** 였다(마진 61% 를 지키는 선). 이제 비용을
    제휴사가 부담하는 구조라 무제한을 약속할 자리가 아니다. 카드 문구가 설계보다
    후하게 적혀 있었던 것을 여기서 바로잡는다.
  - 오늘 몫을 다 쓴 환전 고객에게는 로그인을 권하지 않는다. 로그인해도 늘지 않고
    내일이면 채워지므로, 그렇게 말하면 거짓말이 된다.
- ⚠ 이 표식도 카운터도 브라우저에 있다. 주소창에 직접 쳐 넣으면 통과한다.
  **비용 방어선이 아니라 안내다.** 실제 비용은 서버가 세는 `/api/tts` 상한이 막는다.
  실연동 때는 신한이 서명한 토큰을 서버에서 검증하는 방식으로 올려야 한다.
- 로그인 제공자를 **신한 SOL 하나**로 줄였다. 슈퍼SOL 고객에게 딸려 가는
  서비스라면 계정도 하나로 잇는 편이 사용자에게 단순하다.
- 신한 SOL 은 공개 OAuth 제공자가 아니다. 인가·토큰·프로필 주소를 **지어내지 않고
  환경변수로 비워 뒀다.** 값이 없으면 데모 흐름으로 동작한다. 모르는 주소를
  그럴듯하게 적어 두면 연동된 것처럼 보여 더 위험하다.
- 로그인 버튼 아이콘은 신한 공식 로고가 아니다. 제휴 시 원본 자산으로 교체해야 한다.

### 법적 문서

- `/terms` · `/privacy` 를 실제로 작성했다. 마이크 오디오가 브라우저 벤더 서버로
  전송된다는 사실을 포함한다.
- 로그인 제공자를 바꾸면서 개인정보처리방침의 수집 항목도 정정했다. 쓰지 않는
  제공자를 수집 항목에 적어 두면 그 자체로 틀린 고지다.
- 약관의 "로그인 없이 모든 연습을 이용할 수 있습니다" 도 3회 제한이 생기며
  사실이 아니게 되어 고쳤다.

### 랜딩에서 연습실로 잇기

캐릭터·상황 카드를 누르면 `/practice?scene=…` 으로 바로 들어간다. 예전에는 소개를
읽고 마음이 들어도 연습실에서 같은 카드를 한 번 더 찾아야 했다. 시나리오가 없는
카드는 지우지 않고 "준비 중" 으로 둔다 — 앞으로 만날 것까지 함께 보이게.

---

## 10. 이전 이후 과제 (지금은 하지 않는다)

- **다국어 확장** — 영어 외 언어. 순서가 비용을 가른다. 지금 하면 언어마다
  시나리오·발음 사전·표현 규칙을 새로 써야 하지만(영어 전용 자산만 82개),
  LLM 대화가 붙은 뒤라면 콘텐츠와 교정을 모델이 만들어 비용이 크게 준다.
  음성 배선(`char.lang`)은 이미 언어별로 되어 있어 엔진 수정은 작다.
  환전 통화 ↔ 여행 국가 ↔ 필요한 언어가 이어지므로 금융 연계와도 맞는다.
- CSS Modules 점진 분리
- 실제 AI 대화 연동 (Claude / GPT) — 현재는 시나리오 스크립트 기반
- **사용자 테이블과 학습 기록 서버 저장** — 세션은 서버가 발급하지만 회원 데이터가 없다 (9.5장).
  저장소는 Supabase 로 정했고 아직 연결하지 않았다
- ~~랜딩 페이지의 예시 수치(이용자 수·후기·요금)를 데모임을 명시하도록 정리~~ →
  **2026-08-28~30 완료.** 명시하는 대신 지어낸 것을 걷어냈다. 9.6 참고
- ~~**이용약관·개인정보처리방침 실제 문서**~~ → **2026-08-28 완료.** 9.6 참고
- CSP 를 nonce 방식으로 승격 — 정적 프리렌더를 포기하는 대가가 있어 지금은 하지 않는다
- 테스트 자동화 (Playwright E2E) 및 `textDiff` 단위 테스트 (Phase 2에서 이월)
- `/api/tts` 호출 제한을 **공유 저장소(Vercel KV 등)로 승격** — 지금은 인스턴스 메모리라 전역 상한이 아니다 (9.5장)
- `🎯 따라 말하기` 의 "목표 문장" 안내가 즉시 덮어쓰이는 원본 동작 정리 (5.1 참고)


---

## 11. 콘텐츠 커리큘럼 계획

기존 여행영어 책들의 목차를 조사해 뼈대를 잡았다. 거의 모든 책이 **여행 시간
순서**로 같은 10개 장을 쓴다.

```
1 기본 회화   2 기내      3 도착(입국심사·환전)  4 교통    5 숙소
6 식사       7 쇼핑      8 관광                9 곤란한 상황  10 귀국
```

### 11.1 우리 현황과 구멍

카드 30장을 이 뼈대에 얹어 보면 이렇게 된다.

| 책의 장 | TripTalk |
|---|---|
| 1. **기본 회화** | 🔴 통째로 없음 |
| 2. 기내 | 🟡 카드 1장 (공항 탭에 얹힘) |
| 3. 도착 | 🟢 카드 4장 / 연습 2 |
| 4. 교통 | 🟢 카드 3장 / 연습 1 |
| 5. 숙소 | 🟢 카드 6장 / 연습 1 |
| 6. 식사 | 🟢 카드 6장 / 연습 1 |
| 7. **쇼핑** | 🔴 카드 1장이 교통 탭에 섞임 |
| 8. 관광 | 🟡 카드 2장 |
| 9. 곤란한 상황 | 🟢 카드 5장 / **연습 0 → 돈 트랙으로 일부 해소** |
| 10. 귀국 | 🔴 없음 |

**가장 큰 구멍은 1장 "기본 회화"다.** 모든 책이 예외 없이 맨 앞에 두는데 우리엔
없다. 특히 **못 알아들었을 때 되묻기**(`Sorry, could you say that again more
slowly?`)는 여행영어에서 실제로 가장 많이 쓰는 문장이고, 나머지를 못 해도 이것만
되면 대화가 굴러간다. 초보 이탈을 막는 첫 관문이기도 하다.

### 11.2 💳 돈 트랙 — 책에 없는 구성

**책들은 돈 관련 상황을 일부러 흩어 놓는다.** 환전은 3장, 세금 환급은 7장, 카드
결제는 계산 부분, 분실 신고는 9장. 여행 순서를 따르니 당연한 배치지만, 그 결과
**"돈 때문에 곤란할 때" 를 한 번에 준비할 방법이 없다.**

우리 카드에도 `💱 환전과 면세점`(공항), `🧾 요금 이의 제기`(숙소),
`💳 결제 오류·이중 청구`(돌발)가 세 탭에 흩어져 있었고 **셋 다 연습이 안 됐다.**

이 상황만 모아 **여행 전 구간을 관통하는 11번째 축**으로 세웠다. 책에 없는
구성이고, 금융 플랫폼과의 접점이기도 하다.

| 시나리오 | 키 | 캐릭터 | 난이도 |
|---|---|---|---|
| 💳 카드 결제 거절 | `carddecline` | Maya · 상점 점원 | 중급 |
| 🏧 해외 ATM 인출 실패 | `atm` | David · 은행 직원 | 중급 |
| 💱 환전소에서 | `exchange` | Olivia · 환전 창구 | 입문 |
| 🧾 택스 리펀드 | `taxrefund` | Sofia · 공항 환급 창구 | 심화 |
| 📞 카드 분실 신고 | `lostcard` | Rachel · 카드사 상담원 | 심화 |

카드 결제 거절에는 **현지 통화 결제**(DCC 회피)를, 환전소에는 **수수료 확인**을
넣었다. 환율만 보고 결정하면 손해라는 것을 대화 안에서 알게 하는 것이 목적이다.

### 11.2b 랜딩 카드 — 커리큘럼은 다 보여주고, 배지로 진행을 말한다

랜딩의 "상황별 커리큘럼" 에 카드가 깔려 있는데 실제로 연습되는 것은 일부라,
방문자는 전부 된다고 읽었다. 세 가지를 놓고 골랐다.

| | 방법 | 판단 |
|---|---|---|
| A | 카드에 `연습 가능` / `준비 중` 배지 | **택함** |
| B | 안 되는 카드를 지움 | 섹션이 초라해지고 확장 계획이 안 보인다 |
| C | 섹션 제목만 손봄 | 카드 하나하나는 여전히 되는 것처럼 보인다 |

A 는 거짓이 사라지면서 로드맵이 남는다. "지금 14개, 계획 36개" 는 약점이
아니라 진행 상황이다.

배지를 달려면 카드와 시나리오가 1:1 이어야 하는데 4개가 짝이 없었다.
기본 표현 4개는 여행 상황 탭에 넣을 자리가 없었고, ATM·택스 리펀드는
카드 자체가 없었다. 그래서 **🗣 기본 표현 탭을 신설**하고 카드 2장을 더했다.

  탭 6개 · 카드 36장 · 연습 가능 14장 (통계의 시나리오 수와 일치)

`Situation.scene` 이 있으면 `연습 가능`, 없으면 `준비 중` 이다. 시나리오를
추가하고 카드에 `scene` 만 연결하면 배지가 따라 바뀐다.

### 11.3 제작 순서

| 순위 | 내용 | 상태 |
|---|---|---|
| 1 | 💳 돈 트랙 5개 | ✅ 완료 (2026-08-21) |
| 2 | 기본 표현 4개 — 되묻기 · 숫자와 가격 · 인사와 스몰토크 · 도움 요청 | ✅ 완료 (2026-08-21) |
| 3 | 쇼핑 독립 — 사이즈·색상 / 계산 / 반품·교환 | 다음 |
| 4 | 기내 · 귀국으로 여행 앞뒤 완성 | |

기본 표현은 `SCENES` 첫머리에 둬 선택 화면 맨 앞에 오게 했다. 상황별 표현을
아무리 외워도 상대의 말이 안 들리면 대화가 거기서 끊기기 때문이다.
현재 **14개** — 기본 4 · 상황 5 · 돈 5. 3~4순위까지 하면 22개가 되고
랜딩 통계도 자동으로 따라 올라간다.
시나리오 하나는 5턴 + 모범 답안 10개 + 키워드이고, 엔진은 이미 있어 `scenarios.ts`
데이터 추가와 `SceneKey` 확장만으로 끝난다. 화면 수정은 필요 없다.

### 11.4 참고한 목차

- [1일 1패턴 여행영어](https://www.aladin.co.kr/shop/wproduct.aspx?ItemId=179493983)
- [여행영어 (리빙북스)](https://product.kyobobook.co.kr/detail/S000001431291)
- [착 붙는 여행영어 (시사북스)](https://www.sisabooks.com/langpl/shop/view?book_num=103027)
- [가장 쉬운 여행 영어 (동양북스)](https://www.dongyangbooks.com/book/book_view.asp?menu_1=en&menu_2=en_conver&goods_code=2804)
